DRBD,Corosync,Pacemakerを使ってクラスタを組んでいる場合、
systemdパッケージのアップデートとcorosyncのヘルスチェックの
タイミングがぶつかると、対象サービス（MySQLとか）の状態が見えなくなって
系切り替えが発生してしまうことがある。

2つの処理がぶつからなければ普通にアップデートして終わるので、
必ず発生する訳ではなさそう。

とは言え危険なので、yum updateを定期的に実行している場合は外しておいた方が良さそう。
/usr/bin/yum update -y --exclude=systemd\*
など。
